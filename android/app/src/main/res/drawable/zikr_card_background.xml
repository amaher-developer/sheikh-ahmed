<?xml version="1.0" encoding="utf-8"?>
<!-- The floating dhikr card. Same green and the same motif as the adhan
     screen, at card scale: it has to be recognisable as the same app in the
     second and a half someone glances at it. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="#22402C"
                android:endColor="#16291D"
                android:angle="270" />
            <corners android:radius="22dp" />
            <stroke android:width="1dp" android:color="#B0813A" />
        </shape>
    </item>
    <item android:left="22dp" android:top="22dp"
          android:right="22dp" android:bottom="22dp">
        <bitmap
            android:src="@drawable/khatim_tile"
            android:tileMode="repeat"
            android:alpha="0.30"
            android:gravity="top|start" />
    </item>
</layer-list>
