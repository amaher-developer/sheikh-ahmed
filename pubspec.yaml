name: sheikh_ahmed_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.16.1+35
environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Localization (Arabic default, RTL)
  easy_localization: ^3.0.7

  # State management
  flutter_riverpod: ^2.6.1

  # Prayer times + Qibla
  adhan_dart: ^1.1.0
  flutter_qiblah: ^3.1.0

  # Audio (Quran + radio streaming, background playback)
  just_audio: ^0.9.42
  audio_service: ^0.18.15

  # Local storage (azkar progress, tracker, favorites)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Notifications (azkar reminders, adhan)
  flutter_local_notifications: ^18.0.1
  # Direct dependency because adhan_scheduler.dart imports it to build the
  # TZDateTime that zonedSchedule requires.
  timezone: ^0.10.0

  intl: ^0.20.2

  # Device GPS location for prayer-time calculation
  geolocator: ^14.0.3

  # Persists prayer-time settings (location, calculation method, madhab)
  shared_preferences: ^2.5.5
  share_plus: ^13.3.0
  http: ^1.6.0

  # Local file storage for downloaded (offline) surah audio
  path_provider: ^2.1.5

  # Self-recording for the memorization practice mode
  record: ^6.1.1
  hijri: ^3.0.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.6

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  config:
    enable-swift-package-manager: false

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/translations/
    - assets/icon/
    - assets/images/
    - assets/audio/

  fonts:
    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Variable.ttf
        - asset: assets/fonts/Cairo-Variable.ttf
          weight: 500
        - asset: assets/fonts/Cairo-Variable.ttf
          weight: 600
        - asset: assets/fonts/Cairo-Variable.ttf
          weight: 700
        - asset: assets/fonts/Cairo-Variable.ttf
          weight: 800
    # Amiri Quran (SIL OFL) — the UI font Cairo is missing the Uthmani
    # marks the Quran API returns (sukun U+06E1, waqf signs, superscript
    # madda, the U+08Fx tanween set), so Quranic text rendered in it comes
    # out broken. This face covers them and is designed for Mus'haf text.
    - family: AmiriQuran
      fonts:
        - asset: assets/fonts/AmiriQuran-Regular.ttf
    # Scheherazade New (SIL OFL) — a second Mus'haf face, offered as a
    # choice in the reader and the default. Larger on the body and
    # noticeably clearer than Amiri Quran at the same point size, and it
    # ships a real Bold, which Amiri Quran does not. Its Uthmani coverage
    # was checked against the marks the Quran API returns before adding it:
    # 788 glyphs, all present, versus Amiri Quran's 266.
    - family: ScheherazadeNew
      fonts:
        - asset: assets/fonts/ScheherazadeNew-Regular.ttf
        - asset: assets/fonts/ScheherazadeNew-Bold.ttf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon_source.png"
  min_sdk_android: 21
  remove_alpha_ios: true

flutter_native_splash:
  # #EEE8DC matches the source image's own background exactly (sampled from
  # its corner pixels) so the centered image sits with no visible seam —
  # it's a shade off AppColors.background (#F7F5EF), which is fine for a
  # sub-second splash transition. One splash for both themes: the source
  # image is opaque (no alpha to key out for a dark variant), and a brief
  # light splash before a dark-mode app loads is standard (e.g. iOS launch
  # screens do the same) rather than worth the risk of a bad cutout.
  color: "#EEE8DC"
  image: assets/icon/app_icon_source.png
  android_12:
    color: "#EEE8DC"
    image: assets/icon/app_icon_source.png
